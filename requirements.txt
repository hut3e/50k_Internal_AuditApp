streamlit==1.28.0
supabase==1.0.3
python-dotenv==1.0.0
pandas==2.1.1
matplotlib==3.8.0
uuid==1.30
fpdf2==2.7.8          # Tạo file PDF dễ dàng (phiên bản mới hơn, nên dùng)
python-docx==1.1.0    # Đọc/ghi file .docx (Microsoft Word)
fastapi==0.110.1      # Framework web hiện đại, hiệu năng cao
uvicorn==0.29.0       # ASGI server cho FastAPI
setuptools
